import { OpenGcsButton } from "../components/GcsLinks";

export function OpsConsolePage() {
  return (
    <section>
      <h2>Ops Console</h2>
      <p>GCS opens separately for safety and reliability.</p>
      <OpenGcsButton />
    </section>
  );
}
